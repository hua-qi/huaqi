---
session_id: agent-b8306022
date: 2026-03-31
time_start: 2026-03-31T14:03:47.764Z
time_end: 2026-03-31T14:04:36.999Z
project: users-lianzimeng-workspace-xp
git_branch: main
---

[2026-03-31 22:03:47] [user]: 请详细阅读以下文件，重点关注：
1. /Users/lianzimeng/workspace/xp/src/server.py - MCP 工具定义、参数结构、返回格式
2. /Users/lianzimeng/workspace/xp/src/storage.py - 存储层接口，重点看 ExperienceStore、MetricsStore、VectorStore 的公开方法签名
3. /Users/lianzimeng/workspace/xp/src/knowledge.py - extract_experience、search、record_session、infer_adoption 的函数签名和核心逻辑
4. /Users/lianzimeng/workspace/xp/src/models.py - 所有数据模型定义
5. /Users/lianzimeng/workspace/xp/src/cli.py - cmd_review 的具体实现逻辑

请返回：
- 每个 MCP 工具的完整参数列表
- storage.py 所有类的公开方法列表（方法名 + 参数）
- knowledge.py 中 extract_experience 的完整质量门控逻辑
- models.py 所有 dataclass 的字段
- cmd_review 的交互流程代码
[2026-03-31 22:03:51] [assistant]: 我来详细阅读这些核心文件，为您提供完整的代码结构信息。
[2026-03-31 22:04:36] [assistant]: 现在让我整理并返回完整的分析结果。

## 📋 完整代码结构分析

我已经详细阅读了所有核心文件。以下是完整的代码结构文档：

---

### 1️⃣ **MCP 工具定义 (server.py)** - 完整参数列表

#### **1. extract_experience**
```python
【必填参数】
- task_description: str  # 一句话说清问题描述
- solution_summary: str  # 核心思路摘要
- key_decisions: str     # 关键决策/踩坑点

【可选参数】
- conversation_summary: str    # 对话过程摘要
- tags: list[str]              # 技术栈标签
- related_files: list[str]     # 相关文件路径

【质量门控】
✓ key_decisions 长度 >= 10 字，否则拒绝
✓ solution_summary 长度 >= 20 字，否则拒绝
✓ 检测与已有经验的余弦相似度，>= 0.85 时拒绝（防止重复）
✓ 自动推断技术栈、场景标签
✓ 自动生成向量并保存到 VectorStore

【返回】JSON with fields: status(pending), id, title, type, level, key_decisions, metadata
```

#### **2. search_best_practices**
```python
【必填参数】
- query: str  # 一句话描述任务

【可选参数】
- tags: list[str]              # 技术栈标签过滤
- top_k: int = 3               # 返回条数
- session_id: str              # 会话 ID（强烈推荐，用于 A/B 测试）
- cross_project: bool = False  # 是否跨项目检索

【检索策略】
✓ 纯向量检索（embedding_only）
✓ 10% 对照组随机分配（无经验返回）
✓ 过滤失效/TTL 过期经验
✓ 按余弦相似度排序，阈值 >= 0.5

【返回】
{
  "ab_test_group": "treatment|control",
  "result_shown": bool,
  "results": [...],  # Experience 对象列表
  "strategy": "embedding_only"
}
```

#### **3. record_session**
```python
【必填参数】
- session_id: str              # 与 search_best_practices 中的一致
- task_description: str        # 任务描述
- experience_ids_injected: list[str]  # 注入的经验 ID 列表
- iteration_count: int         # 对话轮数
- had_error_correction: bool   # 是否出现报错修正
- user_accepted: bool          # 用户是否接受

【可选参数】
- ab_test_group: str = "treatment"       # A/B 分组
- result_shown: bool = True              # 是否展示了经验

【返回】
{
  "status": "recorded",
  "session_id": str
}
```

#### **4. record_feedback**
```python
【必填参数】
- experience_id: str  # 经验 ID
- adopted: bool       # 是否被采纳

【可选参数】
- reason: str  # 未采纳原因

【返回】
{
  "status": "recorded|error",
  "experience_id": str,
  "adopted": bool,
  "message": str
}
```

#### **5. infer_adoption**
```python
【必填参数】
- session_id: str                        # 会话 ID
- final_response: str                    # 最终回复内容
- experience_ids_injected: list[str]     # 注入的经验 ID 列表

【推断算法】
✓ 计算 final_response 与各经验的向量余弦相似度
✓ 相似度 >= 0.75 → 标记为采纳
✓ 相似度 0.60~0.75 → 标记为采纳（置信度略低）
✓ 相似度 < 0.60 → 标记为未采纳
✓ 自动调用 record_feedback 记录

【返回】
[
  {
    "experience_id": str,
    "adopted": bool,
    "confidence": float,
    "matched_keywords": [],
    "match_rate": float
  },
  ...
]
```

---

### 2️⃣ **存储层接口 (storage.py)** - 公开方法签名

#### **ExperienceStore 类**
```python
# 增删改查
def add(self, exp: Experience) -> Experience
def get(self, exp_id: str) -> Optional[Experience]
def update(self, exp: Experience) -> bool
def delete(self, exp_id: str) -> bool

# 列表查询
def list_by_status(self, status: ExperienceStatus) -> list[Experience]
def list_active(self) -> list[Experience]
def count_by_status(self) -> dict[str, int]

# 内部方法
def _to_dict(self, exp: Experience) -> dict
def _from_dict(self, d: dict) -> Experience
```

#### **MetricsStore 类**
```python
# 事件记录
def record_search(self, query: str, result_count: int)
def record_review(self, experience_id: str, action: str, reject_reason: Optional[str] = None)
def record_session(self, session: Session)
def record_feedback(self, feedback: Feedback)

# 统计查询
def get_experience_stats(self, exp_id: str) -> Optional[ExperienceStats]
def get_feedback_summary(self, since_days: Optional[int] = None) -> dict
def get_search_miss_queries(self, since_days: int = 30, limit: int = 10) -> list[dict]
def get_recent_query_keywords(self, since_days: int = 30, top_k: int = 20) -> list[str]
def get_experience_feedback_history(self, exp_id: str) -> list[Feedback]
def get_stats(self, since_days: Optional[int] = None) -> dict

# 内部方法
def _update_exp_stats(self, conn: sqlite3.Connection, feedback: Feedback)
```

#### **VectorStore 类**
```python
# 向量存储
def save_vector(self, exp_id: str, vector: list[float])
def delete_vector(self, exp_id: str)
def save_vectors(self, items: list[tuple[str, list[float]]])

# 向量检索
def get_vector(self, exp_id: str) -> Optional[np.ndarray]
def get_all_vectors(self, exp_ids: Optional[list[str]] = None) -> tuple[list[str], np.ndarray]

# 内部
def _get_conn(self) -> sqlite3.Connection
```

---

### 3️⃣ **KnowledgeService 核心逻辑 (knowledge.py)** - extract_experience 完整质量门控

```python
async def extract_experience(
    task_description: str,
    solution_summary: str,
    key_decisions: str,
    conversation_summary: Optional[str] = None,
    tags: Optional[list[str]] = None,
    related_files: Optional[list[str]] = None,
) -> Experience:
    """质量门控流程"""
    
    # 【第一道防线】基础校验
    if not key_decisions or len(key_decisions.strip()) < 10:
        raise ValueError("key_decisions 不能为空且长度不得少于 10 字。请补充关键决策/踩坑点后重试。")
    
    if not solution_summary or len(solution_summary.strip()) < 20:
        raise ValueError("solution_summary 长度不得少于 20 字。请补充解决方案摘要后重试。")
    
    # 【第二道防线】重复检测（基于向量相似度）
    await self._check_duplicate(task_description, solution_summary)  # >= 0.85 拒绝
    
    # 【自动推断】
    level = self._infer_level(task_description, key_decisions)          # 推断难度等级
    exp_type = self._infer_type(task_description, key_decisions)        # 推断类型
    tech_stack = self._infer_tech_stack(all_text)                       # 自动提取技术栈
    scene = self._infer_scene(all_text)                                 # 自动提取场景标签
    
    # 【关键步骤】
    ✓ 计算相关文件的 hash（文件变更检测）
    ✓ 调用 LLM 自动生成标题（fallback 使用截断）
    ✓ 创建 Experience 对象，初始状态为 PENDING
    ✓ 存储到 knowledge.json
    ✓ 调用 embedding provider 生成向量
    ✓ 向量保存到 VectorStore（sqlite3 BLOB）
    
    【初始值】
    - confidence: 0.6（新提取的经验置信度低）
    - status: PENDING（等待人工 review）
    - source: AGENT（来源标记）
```

#### **补充函数：推断引擎**
```python
def _infer_level(task: str, decisions: str) -> ExperienceLevel:
    # 包含"架构/模式/设计" → L1（高难度）
    # 包含"bug/fix/报错" → L3（低难度）
    # 其他 → L2（中难度）

def _infer_type(task: str, decisions: str) -> ExperienceType:
    # 包含"bug/fix/报错" → BUGFIX
    # 包含"模式/架构/规范" → PATTERN
    # 其他 → FEATURE

def _infer_tech_stack(text: str) -> list[str]:
    # 关键词匹配：react, vue, typescript, python, docker, git 等
    # 返回匹配到的技术栈标签列表

def _infer_scene(text: str) -> list[str]:
    # 场景标签字典（40+ 场景）
    # 返回匹配到的场景标签列表
    # 覆盖：表单、列表、异步、性能优化、测试、部署、CLI、数据库、容器化等
```

#### **重复检测流程**
```python
async def _check_duplicate(task_description: str, solution_summary: str):
    # 1. 获取所有 ACTIVE 经验候选集合
    # 2. 使用 embedding provider 生成 task_description + solution_summary 的向量
    # 3. 从 VectorStore 批量获取所有候选经验的向量
    # 4. 计算余弦相似度
    # 5. 如果存在相似度 >= 0.85 的经验 → 抛出异常，拒绝提取
    #    异常信息：已存在相似经验 [ID] 标题，建议用 xp edit 更新
```

---

### 4️⃣ **数据模型 (models.py)** - 所有 Dataclass 字段

#### **ExperienceMetadata**
```python
@dataclass
class ExperienceMetadata:
    tech_stack: list[str] = []     # 技术栈标签
    problem_type: str = ""         # 问题类型（bugfix/feature/pattern）
    scene: list[str] = []          # 场景标签
```

#### **Experience (核心数据模型)**
```python
@dataclass
class Experience:
    # 必需字段
    id: str                                # UUID
    type: ExperienceType                   # BUGFIX | FEATURE | PATTERN
    level: ExperienceLevel                 # L1 | L2 | L3
    title: str                             # 标题（人类可读）
    tags: list[str]                        # 向后兼容标签
    problem: str                           # 问题描述
    solution: str                          # 解决方案描述
    confidence: float                      # 置信度 [0.0~1.0]
    status: ExperienceStatus               # PENDING | ACTIVE | ARCHIVED
    source: ExperienceSource               # AGENT | MANUAL
    created_at: str                        # ISO8601 时间戳
    
    # 可选字段
    related_files: list[str] = []          # 相关文件路径（Phase 3）
    reject_reason: Optional[str] = None    # 拒绝原因
    similarity: Optional[float] = None     # 检索相似度
    metadata: ExperienceMetadata           # 结构化元数据
    file_hashes: dict[str, str] = {}       # 文件 hash 追踪（Phase 3）
    project: str = "default"               # 所属项目
    last_hit_at: Optional[str] = None      # 最后使用时间（TTL）
    stale_reason: Optional[str] = None     # 失效原因
    key_decisions: str = ""                # 关键决策/踩坑点
```

#### **Session (A/B 测试)**
```python
@dataclass
class Session:
    session_id: str                        # 会话唯一标识
    task_description: str                  # 任务描述
    experience_ids_injected: list[str]     # 注入的经验 ID
    iteration_count: int                   # 对话轮数
    had_error_correction: bool             # 是否出现报错修正
    user_accepted: bool                    # 用户接受率
    created_at: str                        # ISO8601 时间戳
    ab_test_group: str = "control"         # 分组：control | treatment
    ab_test_result_shown: bool = True      # 是否展示了经验
```

#### **Feedback**
```python
@dataclass
class Feedback:
    experience_id: str                     # 经验 ID
    adopted: bool                          # 是否采纳
    reason: Optional[str] = None           # 拒绝原因
    created_at: str                        # ISO8601 时间戳（自动生成）
```

#### **ExperienceStats**
```python
@dataclass
class ExperienceStats:
    experience_id: str                     # 经验 ID
    hit_count: int = 0                     # 被检索次数
    adopted_count: int = 0                 # 被采纳次数
    rejected_count: int = 0                # 被拒绝次数
    last_used_at: Optional[str] = None     # 最后检索时间
    last_adopted_at: Optional[str] = None  # 最后采纳时间
    adoption_rate: float = 0.0             # 采纳率 (adopted_count / hit_count)
```

---

### 5️⃣ **cmd_review 交互流程代码 (cli.py)**

```python
def cmd_review(args):
    """详细的交互式 Review 流程"""
    
    # 【初始化】
    from .storage import ExperienceStore, MetricsStore
    from .models import ExperienceStatus
    
    store = ExperienceStore()
    metrics = MetricsStore()
    pending = store.list_by_status(ExperienceStatus.PENDING)
    
    # 【检查】无待审核经验时返回
    if not pending:
        print("没有待 review 的经验。")
        return
    
    # 【循环处理每条经验】
    print(f"\n共 {len(pending)} 条待确认经验\n{'=' * 50}")
    
    for i, exp in enumerate(pending, 1):
        print(f"\n[{i}/{len(pending)}] {exp.title}")
        print(f"  类型: {exp.type.value}  层级: {exp.level.value}  来源: {exp.source.value}")
        print(f"  标签: {', '.join(exp.tags) if exp.tags else '无'}")
        print(f"  ID:   {exp.id}")
        
        print(f"\n  问题:\n  {exp.problem[:300]}")
        print(f"\n  解决方案:\n  {exp.solution[:500]}")
        
        if exp.key_decisions:
            print(f"\n  关键决策/踩坑点:\n  {exp.key_decisions[:300]}")
        
        print(f"\n  元数据:")
        print(f"    技术栈: {', '.join(exp.metadata.tech_stack) if exp.metadata.tech_stack else '无'}")
        print(f"    场景: {', '.join(exp.metadata.scene) if exp.metadata.scene else '无'}")
        
        print("\n  操作: [y] 确认  [n] 拒绝  [s] 跳过  [q] 退出")
        
        # 【用户交互循环】
        while True:
            choice = input("  > ").strip().lower()
            
            # 【确认】
            if choice == "y":
                exp.status = ExperienceStatus.ACTIVE
                exp.confidence = 0.8
                store.update(exp)
                metrics.record_review(exp.id, "confirmed")
                print("  已确认并加入知识库。")
                break
            
            # 【拒绝】
            elif choice == "n":
                reason = input("  拒绝原因（可直接回车跳过）: ").strip() or None
                exp.status = ExperienceStatus.ARCHIVED
                exp.reject_reason = reason
                store.update(exp)
                metrics.record_review(exp.id, "rejected", reason)
                print("  已拒绝并归档。")
                break
            
            # 【跳过】
            elif choice == "s":
                print("  已跳过。")
                break
            
            # 【退出】
            elif choice == "q":
                print("\n已退出 review。")
                return
            
            # 【输入验证】
            else:
                print("  请输入 y / n / s / q")
    
    # 【完成】
    print("\nreview 完成。")
```

#### **核心逻辑流程图**
```
start
  ↓
检查 PENDING 经验是否存在
  ├─ 无 → 打印提示并返回
  └─ 有 ↓
显示经验总数 ↓
for each exp in pending:
    ├─ 显示经验详情（问题/方案/关键决策/元数据）
    ├─ 循环获取用户操作：
    │  ├─ [y] 确认 → status=ACTIVE, confidence=0.8, 记录 review:confirmed
    │  ├─ [n] 拒绝 → status=ARCHIVED, reject_reason, 记录 review:rejected
    │  ├─ [s] 跳过 → 不改状态，继续下一个
    │  └─ [q] 退出 → 终止整个流程
    └─ 继续下一个
结束 → 打印"review 完成"
```

---

### 📊 **关键数据流总结**

| 阶段 | 操作 | 状态变化 | 存储位置 |
|------|------|---------|---------|
| **提取** | `extract_experience` | PENDING | knowledge.json + vectors.db |
| **Review** | `cmd_review` | PENDING → ACTIVE/ARCHIVED | knowledge.json |
| **检索** | `search_best_practices` | 更新 `last_hit_at` | metrics.db |
| **采纳推断** | `infer_adoption` | 自动记录反馈 | metrics.db feedback 表 |
| **反馈** | `record_feedback` | 更新 confidence + 统计 | metrics.db stats 表 |
| **TTL/失效** | `run_ttl_check` | ACTIVE → ARCHIVED | knowledge.json |

---

### 🔑 **关键参数对照表**

```python
# 文件位置
XP_HOME = /Users/lianzimeng/workspace/xp-data/
  ├─ knowledge.json      # 经验主库
  ├─ metrics.db          # 统计 + 向量库
  └─ experience_vectors  # 向量表 (BLOB)

# 数据库表结构
metrics.db 包含：
  ├─ sessions            # 会话记录（A/B 测试）
  ├─ search_events       # 检索事件
  ├─ review_events       # Review 事件
  ├─ feedback            # 反馈记录
  ├─ experience_stats    # 经验统计
  └─ experience_vectors  # 向量存储

# 向量存储格式
struct: {
  experience_id (TEXT)
  vector (BLOB)          # struct.pack('{}f'.format(n), *vector) 压缩
  updated_at (TEXT)
}
```

---

这份文档涵盖了您需要的所有信息！建议您保存这份文档作为开发参考。
