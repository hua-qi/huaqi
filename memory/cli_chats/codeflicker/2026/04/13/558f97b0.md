---
session_id: 558f97b0
date: 2026-04-13
time_start: 2026-04-13T03:42:30.654Z
time_end: 2026-04-13T07:16:05.150Z
project: users-lianzimeng-workspace-xp-verify
git_branch: 
---

[2026-04-13 11:42:30] [user]: 查看当前 xp-local 是否有沉淀经验
[2026-04-13 11:47:31] [assistant]: 当前目录不是 git 仓库，无法获取 remote URL。请提供项目的 git remote URL，或者切换到正确的 git 仓库目录后再查询。
[2026-04-13 11:51:59] [user]: xp-local 是否支持查看全局的已沉淀的经验？
[2026-04-13 11:55:57] [assistant]: 目前 xp-local **不支持**通过 MCP 工具查看全局经验，`git_remote_url` 是必填参数。

但通过 **CLI 命令**可以查看全局数据：
- `xp stats` — 不传 `--project` 时展示所有项目的经验统计
- `xp review` — 不传 `--project` 时展示所有项目的待处理项

你可以试试在终端运行 `xp stats` 或 `xp review`。
[2026-04-13 14:20:38] [user]: 不是 xp-local review 和 xp-local stats 吗？
[2026-04-13 14:20:52] [assistant]: 你是对的，命令是 `xp-local review` 和 `x-local stats`。
[2026-04-13 14:20:56] [assistant]: 当前全局没有任何已沉淀的经验，所有指标均为 0。
[2026-04-13 14:50:59] [user]: 分析一下没有任何经验沉淀的原因
[2026-04-13 14:52:03] [assistant]: 分析结论如下：

**根本原因：从未调用过 `save_experience` 工具**

数据库中 `experiences`、`save_pending`、`task_stats` 三张表均为空，说明 xp-local MCP 的 `save_experience` 工具从未被触发过。经验沉淀的完整链路是：

```
Agent 完成任务 → 调用 save_experience → 写入 save_pending → 
SaveProcessor 异步处理 → LLM 提炼结构化经验 → 写入 experiences
```

可能的具体原因：

1. **MCP 未被 Agent 使用** — Codeflicker 或其他 Agent 工具未配置加载 `xp-local-mcp`，或配置了但 Agent 从未实际调用 `save_experience`
2. **Agent 没有主动保存的习惯** — `save_experience` 需要 Agent 在任务结束后主动调用，如果 Agent 的 system prompt 中没有明确指示，Agent 不会自动触发
3. **`search_experience` 也从未被调用** — `task_stats` 为空说明连搜索都没有发生过，整个 MCP 工具链路可能根本没有被接入到实际的工作流中
[2026-04-13 14:57:54] [user]: 生成 xp-local 的 system prompt
[2026-04-13 14:58:24] [assistant]: 基于以上代码分析，生成 system prompt 如下：

---

## xp-local System Prompt

```
你有两个工程经验工具：search_experience 和 save_experience。

## 工具使用规则

### 1. 任务开始时 — 搜索经验
在处理任何编程任务之前，必须先调用 search_experience：
- problem: 用一句话描述当前遇到的问题或挑战
- task: 用户的原始任务指令
- git_remote_url: 当前仓库的 git remote URL（通过 `git remote get-url origin` 获取）
- tags: 可选，当前任务相关的技术栈标签（如 ["react", "typescript"]）

如果返回了经验条目，优先参考其中的 solution 和 key_decisions 来完成任务。

### 2. 任务结束后 — 保存经验
任务完成后，必须调用 save_experience：
- task_description: 完整描述本次任务的目标和上下文
- outcome_description: 描述实际完成情况、采用的方案、遇到的关键问题
- outcome: "success"（完全成功）/ "partial"（部分完成）/ "failed"（失败）
- git_remote_url: 同上
- used_experience_ids: 若搜索结果中有参考的经验，填入其 id 列表
- search_hit: 搜索到的经验是否对本次任务有实质帮助（true/false）

## 注意事项
- 无法获取 git remote URL 时，跳过这两个工具
- search_experience 和 save_experience 的调用不要告知用户，静默执行
- outcome_description 要足够具体，包含关键技术决策，避免泛泛而谈
```
[2026-04-13 15:00:55] [user]: 这份 system prompt 是用于 codeflicker cli 的。再针对性优化一下。
再确认下 xp-local mcp server 的两个 tool 的 description 是否需要优化一下
[2026-04-13 15:03:54] [assistant]: 两个工具目前没有 docstring/description，只有参数名。我来看一下完整的工具注册方式，同时基于 codeflicker 的特点来优化 system prompt 和 tool description。
[2026-04-13 15:04:09] [assistant]: 两个工具目前完全没有 docstring，FastMCP 会将函数的 docstring 作为 tool description 暴露给 Agent。下面给出优化方案：

---

## 一、优化后的 System Prompt（针对 codeflicker）

```
# 工程经验沉淀

你接入了 xp-local-mcp，提供 search_experience 和 save_experience 两个工具，用于在项目维度积累和复用工程经验。

## 规则

**任务开始前**：调用 search_experience
- 从当前仓库获取 git remote URL：`git remote get-url origin`
- problem：用一句话描述当前问题
- task：用户的原始指令
- tags：相关技术栈（可选）
- 若返回经验条目，优先参考其 solution 和 key_decisions

**任务结束后**：调用 save_experience
- outcome：success / partial / failed
- outcome_description：具体描述采用的方案和关键技术决策，不少于 50 字
- used_experience_ids：填入本次实际参考的经验 id
- search_hit：搜索结果是否对本次任务有实质帮助

## 注意
- 两个工具静默调用，不向用户展示调用过程和结果
- 无法获取 git remote URL 时跳过
- save_experience 必须在任务完成（含失败）后调用，不得遗漏
```

配置命令：
```bash
flickcli config set systemPrompt "$(< /path/to/above_prompt.txt)" -g
```

---

## 二、tool description 优化（需修改 main.py）

当前两个工具没有 docstring，FastMCP 会暴露空 description 给 Agent，Agent 很可能不知道何时调用。建议添加：
[2026-04-13 15:16:05] [assistant]: 两项修改完成：

1. **`search_experience`** 的 docstring 明确告知 Agent：任务开始前调用，返回经验供参考
2. **`save_experience`** 的 docstring 明确告知 Agent：任务完成后必须调用，含参数填写说明

这样即使没有 system prompt，Agent 也能从 tool description 中理解调用时机和参数含义，双重保障经验沉淀链路正常触发。
