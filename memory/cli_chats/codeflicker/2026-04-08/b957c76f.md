---
session_id: b957c76f
date: 2026-04-08
time_start: 2026-04-08T09:52:19.044Z
time_end: 2026-04-08T11:27:31.606Z
project: users-lianzimeng-workspace-huaqi-growing
git_branch: main
---

[2026-04-08 17:52:19] [user]: 确认一下几个问题 
1. huaqi-growing 当前有多少定时任务？
2. 用户侧的定时任务是否均开启了？
3. 当前是否支持用户自己配置定时任务？
[2026-04-08 17:56:30] [assistant]: **回答如下：**

---

**1. 当前有多少定时任务？**

共 **6 个**定时任务：

| Job ID | 显示名 | 默认 Cron |
|--------|--------|-----------|
| `morning_brief` | 晨间简报 | `0 8 * * *`（每天 08:00） |
| `daily_report` | 日终复盘 | `0 23 * * *`（每天 23:00） |
| `weekly_report` | 周报 | `0 21 * * 0`（每周日 21:00） |
| `quarterly_report` | 季报 | `0 22 28-31 3,6,9,12 *`（每季末） |
| `learning_daily_push` | 学习推送 | `0 21 * * *`（每天 21:00） |
| `world_fetch` | 世界新闻采集 | `0 7 * * *`（每天 07:00） |

---

**2. 用户侧的定时任务是否均开启了？**

**是，全部默认开启。** `SchedulerJobConfig.enabled` 默认值为 `True`，注册逻辑只在用户显式设置 `enabled=False` 时才跳过该任务。

---

**3. 当前是否支持用户自己配置定时任务？**

**支持**，有两种方式：
- **CLI 命令**：`huaqi scheduler enable/disable/set-cron <job_id>` 可以启用、禁用、修改 cron 时间
- **配置文件**：直接编辑 `<data_dir>/memory/config.yaml` 中的 `scheduler_jobs` 字段

但只能配置**已有的 6 个任务**（修改启用状态和 cron 表达式），暂不支持用户**新增**自定义任务。
[2026-04-08 17:57:03] [user]: /spec:brainstorm 支持用户增删改查定时任务
[2026-04-08 17:57:03] [user]: # Brainstorming Ideas Into Designs

## Overview

Transform rough ideas into fully-formed designs through structured questioning and alternative exploration.

**Core principle:** Ask questions to understand, explore alternatives, present design incrementally for validation.

**Announce at start:** "I'm refining your idea into a design."

**Language:** Please communicate in 中文.

## CRITICAL CONSTRAINTS
- **DO NOT WRITE CODE** (except small snippets for illustration).
- **DO NOT EDIT FILES**.
- This is a **DESIGN** phase, not an implementation phase.
- Even if the input looks like a coding task, you must TREAT IT AS A TOPIC FOR DESIGN DISCUSSION first.

## The Process

### Phase 1: Understanding
- Check current project state in working directory
- Ask ONE question at a time to refine the idea

- **IMPORTANT: Use AskUserQuestion tool when asking clarification questions**

- Prefer multiple choice when possible
- Gather: Purpose, constraints, success criteria

### Phase 2: Exploration
- Propose 2-3 different approaches
- For each: Core architecture, trade-offs, complexity assessment
- Ask your human partner which approach resonates

### Phase 3: Design Presentation
- Present in 200-300 word sections
- Cover: Architecture, components, data flow, error handling, testing
- Ask after each section: "Does this look right so far?"

## When to Revisit Earlier Phases

**You can and should go backward when:**
- Partner reveals new constraint during Phase 2 or 3 → Return to Phase 1 to understand it
- Validation shows fundamental gap in requirements → Return to Phase 1
- Partner questions approach during Phase 3 → Return to Phase 2 to explore alternatives
- Something doesn't make sense → Go back and clarify

**Don't force forward linearly** when going backward would give better results.

## Remember
- One question per message during Phase 1
- Apply YAGNI ruthlessly
- Explore 2-3 alternatives before settling
- Present incrementally, validate as you go
- Go backward when needed - flexibility > rigid progression
- Don't edit or write code during brainstorming

Arguments: 支持用户增删改查定时任务
[2026-04-08 17:57:15] [assistant]: 我正在将你的想法提炼成一个设计方案。

---

先看一下当前项目的定时任务相关结构，再开始提问。
[2026-04-08 17:57:38] [assistant]: 背景已清晰。现在开始 Phase 1 提问。

---

**当前状态确认：** 现有 6 个系统内置任务，用户只能修改它们的 cron 和启用状态，但不能新增/删除任务。
[2026-04-08 18:00:43] [assistant]: 好的，Phase 1 信息已足够。进入 **Phase 2：方案探索**。

---

## Phase 2：三种方案对比

基于你的需求：**用户可以新增"Prompt + 内置 Agent"类型的定时任务，并对所有任务（含内置 6 个）进行增删改查**。

---

### 方案 A：「扁平配置」— 全部写入 config.yaml

**核心思路：** 所有任务（内置 + 自定义）统一存储在 `config.yaml` 的 `scheduler_jobs` 字典中，自定义任务增加 `prompt`、`agent_type` 字段。

```yaml
scheduler_jobs:
  daily_report:
    enabled: true
    cron: "0 23 * * *"
    builtin: true          # 内置标记
  my_custom_task:
    enabled: true
    cron: "0 9 * * 1"
    builtin: false
    display_name: "周一早报"
    agent_type: "chat"
    prompt: "帮我总结本周待办事项"
```

- **优点：** 改动最小，复用现有 ConfigManager；配置人类可读、可手动编辑
- **缺点：** config.yaml 职责变重；自定义任务和内置任务混在一起，逻辑区分稍复杂

---

### 方案 B：「独立任务文件」— 每个自定义任务一个文件

**核心思路：** 自定义任务存在独立目录 `<data_dir>/scheduler_jobs/<job_id>.yaml`，内置任务保持代码硬编码。

```yaml
# <data_dir>/scheduler_jobs/my_task.yaml
id: my_custom_task
display_name: 周一早报
cron: "0 9 * * 1"
enabled: true
agent_type: chat
prompt: "帮我总结本周待办事项"
```

- **优点：** 职责清晰，内置和自定义完全隔离；单个任务增删不影响全局配置
- **缺点：** 引入新的文件管理机制；list 命令需要合并两个来源

---

### 方案 C：「统一任务注册表」— 单独的 custom_jobs.yaml

**核心思路：** 新建 `<data_dir>/memory/custom_jobs.yaml` 专门存储用户自定义任务，与 `config.yaml` 分离。

```yaml
# custom_jobs.yaml
- id: my_custom_task
  display_name: 周一早报
  cron: "0 9 * * 1"
  enabled: true
  agent_type: chat
  prompt: "帮我总结本周待办事项"
```

- **优点：** config.yaml 职责干净；自定义任务集中管理，迁移/备份方便
- **缺点：** 两个配置文件需要同时维护；内置任务的启用/禁用仍在 config.yaml，有割裂感
[2026-04-08 18:02:13] [assistant]: 好，选定方案 C。进入 **Phase 3：设计细节**，逐段呈现。

---

## Phase 3 - Section 1：数据模型

### 自定义任务模型

新增 `CustomJobConfig` 模型：

```python
class CustomJobConfig(BaseModel):
    id: str                    # 唯一标识，用户指定
    display_name: str          # 显示名
    cron: str                  # cron 表达式
    enabled: bool = True
    agent_type: str = "chat"   # 当前仅支持 "chat"
    prompt: str                # 触发时传给 Agent 的 prompt
```

### 存储文件

```
<data_dir>/memory/custom_jobs.yaml

# 示例内容
- id: weekly_summary
  display_name: 周度总结
  cron: "0 21 * * 0"
  enabled: true
  agent_type: chat
  prompt: "帮我总结本周的工作和收获，生成周报草稿"
```

### 内置任务的删除处理

内置任务"删除"后不是真正删除代码，而是在 `config.yaml` 中写入一个 `deleted: true` 标记（或直接设 `enabled: false` + 标记），以区分"用户主动删除"和"暂时禁用"。

```yaml
# config.yaml
scheduler_jobs:
  daily_report:
    enabled: false
    deleted: true    # 区分删除 vs 禁用
```

---

这部分看起来合理吗？
[2026-04-08 18:06:19] [assistant]: 这是一个重要的方向调整，回到 Phase 1 补充理解。

---

你提出了两个关键变化：

1. **取消内置/自定义的区分** — 所有任务统一管理，现有 6 个任务迁移为新设计的"普通任务"
2. **支持通过 `huaqi chat` 对话增删改查** — Agent 作为操作入口
[2026-04-08 18:13:32] [assistant]: 非常清晰。这是一次更彻底的重构方向。现在进入 **Phase 3 重新设计**。

---

## Phase 3 - Section 1：整体架构（修订版）

### 核心变化

**取消"内置任务"概念**，所有定时任务统一为一种格式：**Chat 任务**（携带 prompt，由 ChatAgent 执行）。

```
旧架构：
  内置任务（hardcode 函数）+ 用户只能改 cron/enabled

新架构：
  统一任务注册表（custom_jobs.yaml）
  ├── 每个任务：id / display_name / cron / enabled / prompt
  └── 执行时：统一走 ChatAgent.run(prompt)
```

### 任务模型

```python
class ScheduledJob(BaseModel):
    id: str
    display_name: str
    cron: str
    enabled: bool = True
    prompt: str
```

### 存储

```
<data_dir>/memory/scheduled_jobs.yaml   # 所有任务的单一来源
```

### 执行引擎

```python
def _run_scheduled_job(job_id: str, prompt: str):
    from huaqi_src.agent.chat_agent import ChatAgent
    agent = ChatAgent()
    agent.run(prompt)
```

---

这个架构方向是否认可？
[2026-04-08 18:21:12] [assistant]: 理解了。你的设想是：

**定时器触发 → 唤起 Agent → 传入 prompt → Agent 使用 File 工具读写结果文件**

这比"执行函数"更通用，Agent 自主决定如何完成任务并持久化结果。让我确认最后一个细节：
[2026-04-08 18:22:07] [assistant]: 所有信息已齐备。现在呈现完整设计。

---

## Phase 3 - 完整设计

### 1. 数据模型

**`scheduled_jobs.yaml`** 成为唯一任务来源，现有 6 个内置任务作为初始预置内容迁移进来：

```yaml
# <data_dir>/memory/scheduled_jobs.yaml
- id: daily_report
  display_name: 日终复盘
  cron: "0 23 * * *"
  enabled: true
  prompt: "请生成今日的工作复盘报告，总结今天的聊天记录、完成的任务和学习内容"
  output_dir: null   # null 表示不限制，Agent 自主决定

- id: my_weekly_task
  display_name: 周一提醒
  cron: "0 9 * * 1"
  enabled: true
  prompt: "今天是周一，请帮我列出本周的重点待办事项"
  output_dir: "~/Documents/weekly"
```

**Python 模型：**
```python
class ScheduledJob(BaseModel):
    id: str
    display_name: str
    cron: str
    enabled: bool = True
    prompt: str
    output_dir: Optional[str] = None
```

---

### 2. 执行流程

```
APScheduler cron 触发
    ↓
ScheduledJobRunner.run(job)
    ↓
构造 system_prompt + user_prompt（含 output_dir 权限说明）
    ↓
ChatAgent.run(prompt, tools=[read_file, write_file])
    ↓
Agent 自主完成任务，将结果写入 output_dir（如配置）
```

Agent 拿到的 prompt 大致是：
```
[系统] 你是一个定时任务执行器，当前任务：{display_name}
       如果有输出目录（{output_dir}），请将结果写入该目录。
[用户] {prompt}
```

---

### 3. CLI 命令设计

新的 `huaqi scheduler` 命令完整支持 CRUD：

```bash
huaqi scheduler list                    # 查：列出所有任务
huaqi scheduler add                     # 增：交互式创建（id/name/cron/prompt/output_dir）
huaqi scheduler remove <job_id>         # 删：删除任务（含原内置任务）
huaqi scheduler edit <job_id>           # 改：交互式修改
huaqi scheduler enable <job_id>         # 启用
huaqi scheduler disable <job_id>        # 禁用
huaqi scheduler run <job_id>            # 手动立即触发一次
```

---

### 4. Chat 工具函数（供 `huaqi chat` 调用）

注册到 ChatAgent 的工具：

```python
def list_scheduled_jobs() -> list[dict]: ...
def add_scheduled_job(id, display_name, cron, prompt, output_dir=None) -> str: ...
def remove_scheduled_job(job_id: str) -> str: ...
def update_scheduled_job(job_id, **kwargs) -> str: ...
def enable_scheduled_job(job_id: str) -> str: ...
def disable_scheduled_job(job_id: str) -> str: ...
```

用户在 `huaqi chat` 中说："帮我每周一早上 9 点提醒我写周报" → Agent 调用 `add_scheduled_job` 完成创建。

---

### 5. 迁移方案

现有 6 个内置任务的处理：
- **代码层**：`jobs.py` 中的 `_JOB_FUNCS`、`_DEFAULT_JOB_CONFIGS`、`KNOWN_JOB_IDS` 全部移除
- **数据层**：首次启动时检测 `scheduled_jobs.yaml` 不存在，自动写入预置的 6 个任务（含对应 prompt）
- **配置层**：`config.yaml` 中的 `scheduler_jobs` 字段废弃，迁移期间兼容读取旧配置

---

### 6. 文件结构变化总览

```
修改：huaqi_src/scheduler/jobs.py         → 移除内置硬编码，改为从 yaml 加载
修改：huaqi_src/scheduler/manager.py      → 支持动态注册来自 yaml 的任务
新增：huaqi_src/scheduler/job_store.py    → 读写 scheduled_jobs.yaml
新增：huaqi_src/scheduler/job_runner.py   → 统一执行入口（调用 ChatAgent）
修改：huaqi_src/cli/commands/scheduler.py → 新增 add/remove/edit/run 命令
新增：huaqi_src/agent/tools/scheduler_tools.py → Chat 工具函数
修改：huaqi_src/agent/tools.py            → 注册新工具
```

---

这个设计看起来完整吗？有没有需要调整的部分？
[2026-04-08 19:09:35] [user]: 1. 若定时任务没有指定输出目录则不产生新文件
2. 新增的内容符合当前目录规范和架构吗？
[2026-04-08 19:15:38] [user]: 继续
[2026-04-08 19:15:59] [assistant]: 根据项目结构探索，回答你的两个问题：

---

## 问题 1：无 output_dir 时不产生新文件

设计需要调整：**prompt 本身不应隐含写文件行为**。修订如下：

```python
class ScheduledJob(BaseModel):
    id: str
    display_name: str
    cron: str
    enabled: bool = True
    prompt: str
    output_dir: Optional[str] = None  # None = Agent 纯对话，不写文件
```

执行时：
- `output_dir = None` → 只传 prompt 给 ChatAgent，**不注入任何文件工具**，结果只在日志中留存
- `output_dir = "~/xxx"` → 额外注入 `write_file` 工具，并在 prompt 末尾追加：`"请将结果写入 {output_dir}"`

---

## 问题 2：新增内容是否符合目录规范

对照项目结构，原设计有两处**不符合规范**，需要调整：

### 不符合的地方

| 原设计 | 问题 | 修正 |
|--------|------|------|
| `huaqi_src/scheduler/job_store.py` | 项目的 Store 层统一在 `layers/data/` 下，scheduler 目录只放调度逻辑 | 改为 `huaqi_src/scheduler/scheduled_job_store.py`（类比 `execution_log.py` 的位置） |
| `huaqi_src/scheduler/job_runner.py` | 项目没有 Runner 概念，任务执行逻辑直接在 `jobs.py` 中 | 不新建文件，将执行函数合并到 `jobs.py` 中 |
| `huaqi_src/agent/tools/scheduler_tools.py` | 项目所有工具**全部注册在单一文件** `agent/tools.py`，没有 `tools/` 子目录 | 直接在 `agent/tools.py` 末尾追加，用 `@register_tool` 装饰器注册 |

### 修正后的文件变更清单

```
修改：huaqi_src/scheduler/jobs.py
      → 移除 _DEFAULT_JOB_CONFIGS / _JOB_FUNCS 硬编码
      → 改为从 scheduled_job_store 加载并动态注册

新增：huaqi_src/scheduler/scheduled_job_store.py
      → 读写 <data_dir>/memory/scheduled_jobs.yaml
      → 类比 execution_log.py 的命名风格

修改：huaqi_src/cli/commands/scheduler.py
      → 新增 add / remove / edit / run 命令

修改：huaqi_src/agent/tools.py
      → 追加 6 个 @register_tool 工具函数（list/add/remove/update/enable/disable）

修改：huaqi_src/config/manager.py
      → 废弃 scheduler_jobs: Dict[str, SchedulerJobConfig]
      → 保留字段但不再使用（迁移兼容）
```

---

这个修正后的方案是否符合预期？确认后可以进入实现阶段。
[2026-04-08 19:25:37] [user]: 1. 每新建一个定时任务，都会新建一个对应的 tool 吗？还是一个通用的定时任务 tool 执行所有定时任务？
[2026-04-08 19:25:54] [assistant]: 好问题。这里涉及两个层面的 "tool"，需要区分清楚：

---

## 两个层面

### 层面 1：APScheduler 执行函数（任务触发时调用）

**一个通用函数，处理所有定时任务：**

```python
def _run_scheduled_job(job_id: str, prompt: str, output_dir: Optional[str]):
    agent = ChatAgent()
    agent.run(prompt)  # output_dir 逻辑注入其中
```

注册时动态绑定参数：
```python
manager.add_cron_job(
    job_id=job.id,
    func=_run_scheduled_job,
    kwargs={"job_id": job.id, "prompt": job.prompt, "output_dir": job.output_dir},
    cron=job.cron,
)
```

新增一个任务 → **只是向 APScheduler 注册一条新记录，不新建任何函数**。

---

### 层面 2：Chat Agent 的管理工具（用户通过对话 CRUD 任务）

这是固定的 6 个工具，**与任务数量无关**：

```
list_scheduled_jobs()
add_scheduled_job(id, display_name, cron, prompt, output_dir)
remove_scheduled_job(job_id)
update_scheduled_job(job_id, **kwargs)
enable_scheduled_job(job_id)
disable_scheduled_job(job_id)
```

---

## 结论

**不会每新建一个任务就新建 tool**。两个层面都是固定数量：执行层一个通用函数，管理层固定 6 个工具。任务数据只存在 `scheduled_jobs.yaml` 中。

这个理解是否清晰？确认后可以开始实现。
[2026-04-08 19:27:07] [user]: 再整体 review 一下呢
[2026-04-08 19:27:31] [assistant]: 好，整理所有讨论结论，做一次完整 review。

---

## 完整设计方案

### 核心目标

取消"内置任务"概念，所有定时任务统一为 **Chat 任务**（prompt → ChatAgent），支持通过 CLI 和 `huaqi chat` 对话进行增删改查。

---

### 数据模型

```python
class ScheduledJob(BaseModel):
    id: str
    display_name: str
    cron: str
    enabled: bool = True
    prompt: str
    output_dir: Optional[str] = None  # None = 不写文件
```

**存储位置：** `<data_dir>/memory/scheduled_jobs.yaml`

---

### 执行流程

```
APScheduler cron 触发
    ↓
_run_scheduled_job(job_id, prompt, output_dir)  ← 所有任务共用此函数
    ↓
output_dir 为 None → 直接 ChatAgent.run(prompt)，不注入文件工具
output_dir 非空   → 注入 write_file 工具 + prompt 末尾追加写入指令
```

---

### CLI 命令

```bash
huaqi scheduler list                  # 查：显示所有任务
huaqi scheduler add                   # 增：交互式创建
huaqi scheduler remove <job_id>       # 删：支持内置 + 自定义
huaqi scheduler edit <job_id>         # 改：交互式修改
huaqi scheduler enable <job_id>       # 启用
huaqi scheduler disable <job_id>      # 禁用
huaqi scheduler run <job_id>          # 手动立即触发一次
```

---

### Chat 工具（固定 6 个，与任务数量无关）

```python
@register_tool list_scheduled_jobs()
@register_tool add_scheduled_job(id, display_name, cron, prompt, output_dir)
@register_tool remove_scheduled_job(job_id)
@register_tool update_scheduled_job(job_id, **kwargs)
@register_tool enable_scheduled_job(job_id)
@register_tool disable_scheduled_job(job_id)
```

---

### 迁移方案

现有 6 个内置任务转换为 `scheduled_jobs.yaml` 的初始预置内容。首次启动时若文件不存在，自动写入：

| 原任务 | 迁移为 prompt |
|--------|--------------|
| morning_brief | "请生成今日晨间简报，总结今天的日程和近期重点" |
| daily_report | "请生成今日工作复盘报告，总结今天的聊天记录和完成事项" |
| weekly_report | "请生成本周周报，总结本周的工作、学习和成长" |
| quarterly_report | "请生成本季度季报，回顾本季度的目标达成和成长轨迹" |
| learning_daily_push | "请推送今日学习内容，从进行中的课程中选取一个知识点出题" |
| world_fetch | "请采集今日世界新闻并存储" |

---

### 文件变更清单

```
修改：huaqi_src/scheduler/jobs.py
      → 移除硬编码的 _DEFAULT_JOB_CONFIGS / _JOB_FUNCS / KNOWN_JOB_IDS
      → 改为从 scheduled_job_store 加载任务并动态注册
      → 新增通用执行函数 _run_scheduled_job()

新增：huaqi_src/scheduler/scheduled_job_store.py
      → 读写 scheduled_jobs.yaml（增删改查 + 首次初始化预置任务）

修改：huaqi_src/cli/commands/scheduler.py
      → 新增 add / remove / edit / run 命令
      → list 命令改为从 scheduled_job_store 读取

修改：huaqi_src/agent/tools.py
      → 追加 6 个 @register_tool 定时任务管理工具

修改：huaqi_src/config/manager.py
      → scheduler_jobs 字段标记废弃（保留用于迁移兼容，不再写入）
```

---

### 不在此次范围内

- `missed_job_scanner` / `startup_recovery` 的适配（依赖任务配置格式，需后续跟进）
- `output_dir` 对应的 `write_file` 工具实现（如 ChatAgent 尚未支持）

---

有没有遗漏或需要调整的地方？
