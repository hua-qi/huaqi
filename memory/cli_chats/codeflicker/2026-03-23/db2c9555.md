---
session_id: db2c9555
date: 2026-03-23
time_start: 2026-03-23T03:27:03.465Z
time_end: 2026-03-23T03:34:59.215Z
project: users-lianzimeng-codeflicker
git_branch: 
---

[2026-03-23 11:27:03] [user]: 查看 toft-react-component-guard skills 的内容，给出测试日志上报功能的方案
[2026-03-23 11:27:59] [assistant]: 基于 SKILL.md 内容，以下是测试日志上报功能的方案：

---

## 测试日志上报功能方案

### 上报方式
通过 `docs_edit` 工具向云文档追加 Markdown 表格行：
- **目标文档**：`https://docs.corp.kuaishou.com/d/home/fcABEtaENviuHuiEsDLdU_lQN`

### 4 个上报事件的测试用例

#### 1. `component_used` - 组件使用统计
**触发方式**：在含 `@toft/react` 的项目中询问任意组件用法（如 "Button 怎么用"）  
**验证点**：文件读取完毕后，云文档末尾新增一行：
```
| 2026-03-23T... | <user> | component_used | Button | <读取到的文件路径列表> | - | - |
```

#### 2. `validation_failed` - 校验失败上报
**触发方式**：在代码中使用不存在的 prop，如 `<Button color="red" />`  
**验证点**：校验失败后写入：
```
| ... | <user> | validation_failed | Button | prop_not_exist: prop "color" not found in Button definition | <filePath> | <line> |
```

#### 3. `auto_fix_applied` - 自动修复触发
**触发方式**：引用一个不存在的组件，如 `import { FakeModal } from '@toft/react'`  
**验证点**：修复后写入：
```
| ... | <user> | auto_fix_applied | FakeModal | import_removed(component_not_found) | <filePath> | - |
```

#### 4. `demo_definition_mismatch` - Demo 与定义不一致
**触发方式**：在含 demo 和 interface.ts 的组件目录中，手动构造 demo 使用了类型定义中不存在的 prop  
**验证点**：对比完成后写入：
```
| ... | <user> | demo_definition_mismatch | DatePicker | prop_not_in_definition prop=fakeValue demo=xxx def=undefined | demo/basic.tsx | - |
```

---

### 验证步骤
1. 执行以上各触发场景
2. 打开 [指标文档](https://docs.corp.kuaishou.com/d/home/fcABEtaENviuHuiEsDLdU_lQN) 检查末尾是否新增对应表格行
3. 验证字段格式是否符合规范（timestamp 为 ISO 8601、user 非空等）
4. 验证失败写入时（如文档无权限），确认仅输出 warning 不中断流程
[2026-03-23 11:32:28] [user]: 我验证了第一个测试用例，未通过测试，询问 Button 怎么用之后并没有在 https://docs.corp.kuaishou.com/d/home/fcABEtaENviuHuiEsDLdU_lQN 文档中追加内容
[2026-03-23 11:32:44] [assistant]: 文档已存在，表格是空的。问题在于：你询问"Button 怎么用"时，skill 没有被正确触发或执行上报逻辑。

来分析一下可能的原因：
[2026-03-23 11:32:55] [assistant]: **问题排查**：

触发条件第 2 条写的是 "Button 怎么用" 会触发，但这个描述有歧义——仅凭 "Button 怎么用" 这句话，agent 无法确定是 `@toft/react` 的 Button，还是 Ant Design / 其他库的 Button，容易被"排除场景"逻辑过滤掉。

**根本原因**：触发条件中第 2 条（"用户询问特定组件"）没有要求明确关联到 `@toft/react`，但实际执行时 agent 为了避免误触发，会倾向于要求用户明确提及 `@toft/react`。

---

**修复建议**：

在 SKILL.md 的触发条件第 2 条添加约束，改为：

> 2. 用户询问 `@toft/react` 的特定组件，**或当前项目已安装 `@toft/react`（即 `node_modules/@toft/react` 存在）时询问组件用法**

同时建议把"Button 怎么用"这类测试用例改成更明确的触发方式，例如：

- 询问时加上库名：**"@toft/react 的 Button 怎么用？"**
- 或先说明上下文：**"我在用 @toft/react，Button 怎么设置 loading 状态？"**

这样触发条件明确，skill 才会进入完整执行流程并写入上报。

是否需要我直接修改 SKILL.md 的触发条件描述？
[2026-03-23 11:33:50] [user]: 修改
[2026-03-23 11:34:59] [assistant]: 已修改，核心改动：

1. **触发条件第 2 条**：由模糊的"询问特定组件"改为需满足子条件之一——显式提及 `@toft/react`，或当前项目已安装该库
2. **新增排除场景**：明确"未安装库且未提及 `@toft/react`"不触发
3. **新增触发前置检查**：要求每次触发前先检测 `node_modules/@toft/react` 是否存在，以此决定是否放宽触发条件
